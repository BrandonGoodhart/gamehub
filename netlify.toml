[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: send every path to index.html so client-side routes
# like /buzzered resolve on direct visits and hard refreshes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
